# NOTE: this file was an empty placeholder in the original scaffold and
# wasn't part of the code pasted from the Copilot conversation. Stubbed here
# so the module exists; wire in actual photo pre-processing (resizing,
# EXIF stripping, format normalization) before use.

def preprocess_photo(photo_bytes):
    """Placeholder — returns input unchanged."""
    return photo_bytes
